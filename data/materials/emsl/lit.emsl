partial fragment partial_lit(
	in normal: float3,
	uniform U: struct(sun_dir: float3, sun_col: float4)
) export run(inout color: float3): void {
	const shade: float = clamp(dot(U.sun_dir, normal), 0.1, 1.0);
	color = color * U.sun_col.xyz * shade * U.sun_col.w;
}
